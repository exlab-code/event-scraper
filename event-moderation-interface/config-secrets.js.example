// Secret configuration values for the Event Moderation Interface
// This is an example file. Copy this to config-secrets.js and update with your actual values.
// The config-secrets.js file should NOT be committed to Git.

// Override the default configuration with actual values
Object.assign(CONFIG.api, {
    baseUrl: 'https://your-directus-api-url',
    token: 'your-api-token-here'
});
